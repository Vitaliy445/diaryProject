@model IEnumerable<diary.Models.Worker>

@{
    ViewData["Title"] = "Home Page";
}
<head>
    <style>
        #tbl {
            width: 100%;
            border: 5px solid black;
        }

        #trTitle {
            background: cornflowerblue;
            border: 5px solid black;
            color: white;
        }

        td {
            font-family: Montserrat;
            font-weight: bold;
        }
    </style>
</head>
<h3>Users</h3>
<table class="table-condensed" border="1" id="tbl">
    <tr id="trTitle">
        <td>Name</td>
        <td>Position</td>
        <td>Department</td>
        <td>Status</td>
        <td>Salary</td>
        <!--if (/*admin || moderator == 1*/)-->
        <!--{
    <td>Edit!</td>
    <td>Delete!</td>
        }
    -->

    </tr>
        @foreach (var animal in Model)
        {
            <tr>
                <td>@Worker.FirstName + @Worker.FirstName + @Worker.MiddlName + @Worker.LastName</td>
                <td>@Worker.Position</td>
                <td>@Worker.Department</td>
                <td>@Worker.Status</td>
                <td>@Worker.Salary</td>
                <td><a href="~/Home/Buy/@Worker.Id">Buy</a></td>
                <td><a href="~/Home/Update/@Worker.Id">Edit</a></td>
                <td><a href="~/Home/DeleteAnimal/@Worker.Id">Delete</a></td>
            </tr>
        }
    </table>
    <br />
    <br />
    <br />
